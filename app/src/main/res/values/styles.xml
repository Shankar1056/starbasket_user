<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorPrimary</item>
        <item name="android:windowBackground">@color/colorBackground</item>
        <item name="android:windowExitAnimation">@null</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:textColor">@color/grey_600</item>
        <item name="android:windowEnterAnimation">@null</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="FullscreenActionBarStyle" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/black_overlay</item>
    </style>

    <style name="AppTheme.Toolbar.TitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">@dimen/text_normal</item>
    </style>

    <style name="TextAppearance.App.TextInputLayout" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/grey_400</item>
    </style>

    <style name="NavigationText" parent="TextAppearance.AppCompat.Small">
        <item name="android:drawablePadding">@dimen/thick</item>
        <item name="android:gravity">bottom</item>
        <item name="android:paddingBottom">@dimen/thick</item>
        <item name="android:paddingEnd">@dimen/xxlarge</item>
        <item name="android:paddingStart">@dimen/xxlarge</item>
        <item name="android:paddingTop">@dimen/thick</item>
        <item name="android:textSize">@dimen/text_very_small</item>
        <item name="android:fontFamily">@font/celias_medium</item>
    </style>

    <style name="AppThemeDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:background">@color/white</item>
        <item name="android:layout_gravity">bottom</item>
    </style>

    <style name="TabLayout" parent="TextAppearance.Design.Tab">
        <item name="textAllCaps">false</item>
        <item name="android:fontFamily">@string/font_medium</item>
    </style>

    <style name="TabLayout1" parent="TextAppearance.Design.Tab">
        <item name="textAllCaps">false</item>
        <item name="android:fontFamily">@string/font_medium</item>
        <item name="android:textSize">@dimen/text_very_small</item>
    </style>

</resources>
